import { BpmnDirective } from './bpmn.directive';

describe('BpmnDirective', () => {
  it('should create an instance', () => {
    const directive = new BpmnDirective();
    expect(directive).toBeTruthy();
  });
});
